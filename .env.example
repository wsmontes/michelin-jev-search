# OPCIONAL. A chave normalmente se cola na própria página, no campo do alto,
# e fica só no localStorage do navegador. Este arquivo serve para quem roda
# local e prefere não colar a cada sessão: o servidor a usa como padrão quando
# a requisição não traz nenhuma.
# Crie a sua em https://console.typesafe.ai/keys
TYPESAFE_AI_API_KEY=
